- The projects for the CMS EPC Demonstration Exercise are cmshealth which is Spring client application that is a consumer of the File Upload Service.

- The File Upload Service is a set of services contained in the qamservices directory.

- Both projects build with Gradle

- Also of imprtance is the FILE_UPLOAD_SERVICE.log that shows the output of the LogMonitor object that is monitoring the File Upload Chain of Responsibility pattern inside the service is called and runs.

- Additionally is the Excel file used for testing.